# Development Dockerfile for frontend - runs Vite dev server
# This allows hot module reload (HMR) and shows startup logs in docker-compose

FROM node:22-alpine

WORKDIR /app

ENV NODE_ENV=development
ENV VITE_HOST=0.0.0.0
ENV VITE_API_BASE_URL=http://localhost:8080

# Copy package files
COPY package*.json ./

# Install all dependencies (including dev)
RUN npm ci

# Copy source code
COPY . .

# Expose Vite dev server port
EXPOSE 5173

# Start Vite dev server
CMD ["npm", "run", "dev", "--", "--host"]
